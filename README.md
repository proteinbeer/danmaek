# 단맥 (Danmaek)

한국 & 영미권 뉴스 큐레이션 블로그

## 로컬 개발

```bash
# Jekyll 설치 필요
gem install jekyll bundler
bundle exec jekyll serve
```

## 글 쓰기

`_posts/` 디렉토리에 `YYYY-MM-DD-title.md` 형식으로 파일 생성.  
`categories: korean` 또는 `categories: english` 로 언어 구분.