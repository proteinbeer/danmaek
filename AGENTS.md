# Danmaek

## 이미지 추가 규칙
- **모든 이미지는 해외 사이트에서 가져온다.**
- **모든 글은 다른 글에서 이미 쓴 이미지를 재사용할 수 있다. 굳이 새 이미지를 찾지 않아도 된다.**
- 새 이미지는 넣기 전에 항상 압축: 최대 가로 1280px로 리사이즈, JPEG 품질 82 정도로 저장
- 사진/스틸 컷은 절대 PNG로 저장하지 말 것 (JPG 사용)
- 목표: 이미지 1장당 300KB 이하
- 이미지 파일은 `public/assets/posts/` 아래에 저장하고, 글에서는 `/assets/posts/...` 경로로 참조

## 게시글(IT/게임/쿠폰) 템플릿 규칙
IT 글이든 게임 글이든 쿠폰 글이든 **모든 게시글은 이 템플릿을 그대로 적용**한다. 새 글 작성 시 아래 규칙을 반드시 따른다.
템플릿 기준: `src/content/posts/monitor-no-signal-guide.md`(IT), `src/content/posts/steam-offline-mode-guide.md`(게임) 등 기존 글.

### 이미지 기본 원칙 (모든 글 공통)
- **대표 이미지 1장은 인트로 문단 바로 아래(첫 소제목 직전)에 배치한다.** frontmatter `image`와 동일한 파일을 사용하고 class에 `post-landscape-image--high`를 추가한다.
- **게임 가이드/쿠폰은 대표 이미지를 하나로 통일한다.** 같은 게임(시리즈)의 가이드·쿠폰 글은 모두 같은 대표 이미지 파일을 사용한다.
- **본문 이미지는 메인(대표) 이미지 1장만 배치하고, 소제목 사이에는 쿠팡 배너를 소제목 2의 배수 섹션 본문 아래마다 배치한다.** (아래 "쿠팡 배너 배치" 규칙 참고)
- **모든 이미지는 `public/assets/posts/` 아래에 저장하고 `/assets/posts/...` 경로로 참조한다.**

### frontmatter
- `category`: 카테고리 이름 (예: "IT", "게임", "쿠폰")
- `tags`: 문자열 배열
- `image`: 로컬 이미지는 `/assets/posts/...` 경로 사용
- `imageAlt`, `imageWidth`, `imageHeight`: 이미지와 함께 반드시 지정
- `hideHeroImage: true`, `hideDescription: true`: IT/게임/쿠폰 글은 히어로 이미지와 설명 자동출력을 끔

### 본문 구조 (순서 지키기)
1. 맨 위에 제휴 공시: `<p class="affiliate-disclosure">이 게시물은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.</p>`
2. 인트로 문단
3. 이미지 삽입: `<img class="post-landscape-image" src="/assets/posts/..." alt="..." width="1280" height="720" loading="lazy" decoding="async" />` (첫 이미지는 class에 `post-landscape-image--high` 추가 가능)
4. 소제목은 번호 매기기: `## 1. ...`, `## 2. ...`
5. **숫자 범위에 반각 물결표(`~`)를 쓰지 않는다.** 마크다운 렌더러가 반각 `~`를 취소선으로 처리한다. "2~3개"로 쓰면 "3개" 쪽이 줄 그어져 보이므로 전각 `～`(2～3개) 또는 "2개에서 3개"처럼 문장으로 쓴다.

### 광고 배치 3단 구조 (모든 글 공통)
- **맨 위:** 글 제목/메타 아래에는 레이아웃의 애드핏 상단 배너를 유지한다. (`src/layouts/PostLayout.astro`의 `adfit-banner--hero`)
- **본문 중간:** 글 본문에는 작은 쿠팡 파트너스 배너를 소제목 2의 배수(`## 2`, `## 4`, `## 6`, ...) 섹션 본문 아래에 넣는다. 순서는 `짝수 소제목 → 해당 본문 → 작은 쿠팡 배너 → 다음 소제목`이다.
- **맨 밑:** 공유 버튼과 다음/이전 글 버튼 아래에는 가로 긴 쿠팡 파트너스 배너를 유지한다. (`src/layouts/PostLayout.astro`의 `coupang-banner`, 662x140)
- 이 3단 구조는 애드센스 심사 때문에 제거하지 않는다. 사용자가 명시적으로 빼라고 하기 전까지 유지한다.

### 대표 이미지 구성 규칙 (모든 글 공통)
- 인트로 문단 바로 아래(첫 소제목 직전)에 대표 이미지 1장을 배치한다. frontmatter `image`와 동일한 파일을 사용하고 class에 `post-landscape-image--high`를 추가한다.
- "이미지 추가 규칙"(1280px, JPEG 82, 300KB 이하)을 준수한다.

### 본문 이미지(스크린샷) 규칙 — 현재 미사용
- **소제목 사이 본문 이미지(스크린샷/스틸컷)는 삽입하지 않는다.** 모든 뉴스/가이드/쿠폰 글에 적용된 규칙이다.
- 글의 이미지는 메인(대표) 이미지 1장뿐이다. 이전에 구성했던 게임별 스크린샷 풀(발하임/던워커/뮤제닉스/귀무자/메이플키우기/랜덤다이스/네트워크/PC하드웨어/스팀 등)은 본문에 더 이상 사용하지 않는다.
5. 작은 쿠팡 배너 배치(소제목 2의 배수 섹션 본문 아래마다 삽입). 기본 원칙:
   - **소제목 2, 4, 6, ... 섹션의 본문이 끝난 뒤 작은 쿠팡 배너를 배치하고, 홀수 소제목 섹션 뒤에는 배너를 넣지 않는다.**
   - **배치 순서는 반드시 `## 2 소제목 → 2번 본문 → 작은 쿠팡 배너 → ## 3 소제목`처럼 유지한다.**
   - **본문 소제목 사이에는 스크린샷/스틸컷 등 이미지를 삽입하지 않는다.** 글의 이미지는 메인(대표) 이미지 1장뿐이다.
   - 소제목 사이 쿠팡 배너는 `margin: 38px 0 30px` 스타일을 사용한다.
   - **본문 안에는 별도의 마지막 배너를 추가하지 않는다.** 본문 끝은 텍스트로 마무리하고, 글 하단 가로 쿠팡 배너는 레이아웃(`PostLayout.astro`)에서 자동으로 출력한다.
   ```html
   <div style="margin: 38px 0 30px; text-align: center;">
     <div style="width: 250px; max-width: 100%; margin: 0 auto; overflow: hidden;">
   <!-- COUPANG PARTNERS DYNAMIC BANNER START -->
   <script src="https://ads-partners.coupang.com/g.js"></script>
   <script>
   	new PartnersCoupang.G({"id":1021110,"template":"carousel","trackingCode":"AF7638395","width":"250","height":"250","tsource":""});
   </script>
   <!-- COUPANG PARTNERS DYNAMIC BANNER END -->
     </div>
   </div>
   ```
6. ~~마지막 참고자료는 반드시 h2 헤딩으로 `## 출처`~~ **(삭제됨: 글에 출처 섹션을 넣지 않는다.)**
7. ~~참고자료 항목은 링크 목록~~ **(삭제됨)**
8. **모든 게시글(뉴스/가이드/쿠폰)에 `## 출처`(또는 참고한 자료) 섹션을 넣지 않는다.** 글 본문만으로 마무리하고, 쿠팡 배너는 그대로 유지한다.
9. **`## 단맥 포인트` 같은 브랜드성 정리 섹션은 넣지 않는다.** 글이 AI 템플릿처럼 보이지 않도록 본문 흐름 안에서 자연스럽게 마무리한다.
10. **뉴스 글에는 본문 시작 전에 `<div class="dan-point">` 요약 박스를 넣는다.** 이름은 `단 포인트`로 쓰고, 보통 3문장으로 정리한다. 앞 1～2문장은 핵심 사실 설명, 마지막 1문장은 짧은 해석이나 판단으로 쓴다. `단맥에서는` 같은 브랜드 주어는 쓰지 않는다. 가이드와 쿠폰 글에는 넣지 않는다.

### 관련 글 섹션 (레이아웃 자동 렌더링)
- 게시글 하단의 관련 글 섹션 헤딩은 **"단 글"** 로 고정됨. (부제목 없음. `src/layouts/PostLayout.astro`에서 관리)
- 이 헤딩 텍스트를 변경하고 싶을 때만 해당 파일을 수정한다.

## 문체 규칙 (모든 게시글 공통)
- 모든 게시글(뉴스/가이드/쿠폰)의 문장 어미 비율은 **"~합니다/~입니다/했습니다" 계열 약 70%, "~죠/~하죠/~이죠/~되죠" 계열 약 30%**로 유지한다.
- 글을 작성하거나 리라이트할 때 문단 여러 개가 연속으로 "~합니다/~입니다"만 반복되지 않게 한다. 보통 3문장 안팎마다 `~죠`, `~하죠`, `~이죠`, `~되죠`, `~봤죠`, `~보이죠` 등을 섞어 단맥 말투를 유지한다.
- 단, `죠`를 억지로 모든 문장에 붙이지 않는다. 정보 전달 문장은 `합니다/입니다`로 안정적으로 두고, 해석·강조·정리 문장에 `죠` 계열을 넣어 전체 비율을 70:30에 가깝게 맞춘다.
- **"요" 어미 금지.** "~해요, ~있어요, ~많아요, ~보세요"뿐 아니라 **"~니까요, ~고요, ~할까요" 등 문장이 '요'로 끝나는 해요체 어미도 쓰지 않는다.**
- 문장 마무리는 "~습니다/~합니다"와 "~죠/하죠/되죠" 위주로 쓴다.

## 쿠폰 글 문체 규칙
- **"요" 어미 금지** (~해요, ~있어요, ~많아요, ~보세요 등 사용 금지)
- "~죠/하죠/했죠" 어미를 문장 약 30% 비중으로 넣고, 나머지는 "~습니다/~합니다"로 쓴다.
- **소제목(##)은 문장을 넣지 않는다.** 명사형/명사구로만 쓴다. (예: "코드별 입력 환경과 유효 기간 주의")
- **제목에 "입력 방법", "리딤코드와 입력 방법" 등 입력 방법 관련 문구를 넣지 않는다.** 제목은 "게임명 쿠폰 N월 최신 정리" 형태로 끝낸다. (예: "원신 쿠폰 9월 최신 정리")
- 이 규칙은 쿠폰 글 작성/수정 시 계속 적용한다.

## 글 작성(복붙 금지) 규칙
- **절대 원본 글을 그대로 복사하거나 문장 단위로 그대로 옮겨 쓰지 않는다.** (의역도 사실상 복사로 간주)
- 여러 글을 참고할 때는 각 자료에서 필요한 사실·수치만 뽑아 **내용을 섞고 다시 조합**해, 어느 한 원문과 겹치지 않는 새로운 문장으로 쓴다.
- 표현·전개·구성까지 바꿔서 단맥만의 어투(문체)로 작성한다. 참고 자료는 사실 확인용으로만 사용한다.
- 이 규칙은 뉴스/가이드/쿠폰 등 모든 게시글 작성 때 적용한다.

## 자료 검색 규칙
- **뉴스·가이드 글은 해외에서 검색한다.** 여러 출처를 섞어서 쓰고, 한 곳에서만 가져오면 티가 나므로 반드시 다수의 출처를 조합한다.
- 출처가 하나밖에 확보되지 않으면, 그 내용을 그대로 옮기지 말고 문장 구성·표현·전개를 완전히 다르게 바꿔 원문처럼 보이지 않게 섞어 쓴다.
- **쿠폰 글만 한국에서 검색한다.**

## IT·게임 글 문체 규칙
- 쿠폰 글 규칙과 동일하게, IT·게임 글도 **소제목(##)은 문장이 아닌 명사형/명사구**로만 쓴다. (예: "직업 계열 선택: 원거리 광역 우선", "다이아 사용 우선순위")
- "~고를까", "~다르다", "~쓴다", "~모은다" 같은 문장형 소제목 금지.
- IT·게임 글에도 "요" 어미 금지 (~하세요, ~잡으세요 등 사용 금지). 문장 마무리는 "~습니다/~합니다/~죠" 위주로 쓴다.

## 커밋/푸시 규칙
- **커밋·푸시 여부를 묻지도 않고, 혼자(스스로) 하지도 않는다.**
- **푸시는 사용자가 "푸시 ㄱㄱ"라고 명시적으로 말할 때만 한다.** 그 외에는 절대 커밋·푸시하지 않는다.
- **빌드(npm run build / seo:check)는 푸시할 때만 실행한다.** 작성·수정 직후에는 빌드하지 않고, 푸시 지시가 있을 때 한 번에 빌드·점검 후 커밋·푸시한다.
- 작업이 끝나면 커밋·푸시 없이 결과만 보고한다.
- 네가 쓰는 글/변경 사항에 대한 확인은 항상 사용자가 먼저 한다. 확인 없이 푸시하지 않는다.

## 뉴스 발행 중단 (애드센스 승인 전까지)
- **애드센스 승인이 나기 전까지 새 뉴스 글을 작성하거나 발행하지 않는다.** (2026-09-16 기준 중단 상태)
- **현재 뉴스 글은 전부 `draft: true`로 놓아둔다.** 뉴스 메뉴(/news/), 홈 뉴스 병합, 사이트맵, RSS에서 뉴스가 제외된 상태를 유지한다.
- 뉴스 글이 필요한 상황이면 IT·게임·쿠폰으로만 작업한다.
- 승인 후 뉴스를 다시 살릴 때는 `scripts/set-news-drafts.mjs`(또는 수동)로 `draft: true`를 제거하고, 메뉴/사이트맵/카테고리 페이지 복원이 필요하면 이어서 진행한다.

## 카테고리 구조 (IT/게임/쿠폰/도구)
- 포스트의 `category`는 **`IT` | `게임` | `쿠폰`** 세 가지만 사용한다. (과거 '가이드' 카테고리는 제거됨)
- **IT 서브카테고리**: 윈도우 11, PC·하드웨어, 네트워크, 주변기기 → URL `/it/{slug}/` (워드프레스/웹 호스팅·SSL·도메인 등 웹 운영 글은 제거됨)
- **게임 서브카테고리**: 스팀, 플스, 스위치, 메이플 키우기, 랜덤 다이스 2, 뮤제닉스, 더 블러드 오브 던워커, 귀무자, 발하임, 스타워즈 제로 컴퍼니, 워독스, 프라그마타, 낚시 방법, 봄바나나, 모탈 셸 2, 할로윈 더 게임, 몬스터헌터 와일즈 → URL `/games/{slug}/`
- 서브카테고리 목록은 `src/consts.ts`의 `IT_SUBCATEGORIES`/`GAME_SUBCATEGORIES`에서 관리한다. IT/게임 판정은 위 서브카테고리 기준을 따른다.
